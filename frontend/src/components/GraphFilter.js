import React from 'react';
import CountryFlagSelection from './CountryFlagSelection';

const GraphFilter = () => {
  return (
    <div>
      <h2>Graph Filter</h2>
      <CountryFlagSelection />
      {/* You can add more filter options here as needed */}
    </div>
  );
};

export default GraphFilter;
