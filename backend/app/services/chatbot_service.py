class ChatbotService:
    @staticmethod
    def get_response(message):
        # Placeholder for more complex chatbot logic
        # For now, it just echoes back the received message
        return f"Chatbot echo: {message}"
